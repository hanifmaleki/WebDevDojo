extends ../layout.pug
include mixin/video-preview

block main
    input(class='c5-searchbar', type='text', placeholder='Search')

    - const videos = [youtubeData[0], youtubeData[1]] 

    each video in videos
        +video-preview(video)
    
